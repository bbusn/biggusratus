[tool.poetry]
name = "biggusratus"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"
pre-commit = "^3.0"
black = "^24.0"
ruff = "^0.3.0"